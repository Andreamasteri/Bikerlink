name: Smoke Test

on:
  schedule:
    # GitHub cron uses UTC. Runs every 10 minutes.
    - cron: "*/10 * * * *"
  workflow_dispatch:
    inputs:
      base_url:
        description: "Optional candidate/preview HTTPS URL to smoke-test (overrides SMOKE_CANDIDATE_BASE_URL)"
        required: false
        type: string

permissions:
  contents: read

concurrency:
  group: bikerlink-candidate-smoke
  cancel-in-progress: false

jobs:
  candidate-smoke:
    name: Candidate API smoke
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment: candidate

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: "22.23.1"
          cache: npm

      - name: Verify smoke configuration
        env:
          BASE_URL: ${{ inputs.base_url || secrets.SMOKE_CANDIDATE_BASE_URL }}
          BIKERLINK_DEPLOY_ENV: staging
          DATABASE_URL_CANDIDATE: ${{ secrets.DATABASE_URL_CANDIDATE }}
          SMOKE_REGISTRATION_TOKEN: ${{ secrets.SMOKE_REGISTRATION_TOKEN }}
        run: |
          test -n "$BASE_URL" || { echo "::error::Missing secret SMOKE_CANDIDATE_BASE_URL"; exit 2; }
          test -n "$DATABASE_URL_CANDIDATE" || { echo "::error::Missing secret DATABASE_URL_CANDIDATE"; exit 2; }
          test -n "$SMOKE_REGISTRATION_TOKEN" || { echo "::error::Missing secret SMOKE_REGISTRATION_TOKEN"; exit 2; }
          node -e '
            const u = new URL(process.env.BASE_URL);
            if (u.protocol !== "https:") throw new Error("Candidate smoke requires HTTPS");
            if (!/(staging|candidate|pr-\d+)/i.test(u.hostname)) {
              throw new Error("Refusing host without staging/candidate/PR marker: " + u.hostname);
            }
          '

      - name: Install dependencies
        run: npm ci

      - name: Run API smoke
        env:
          BASE_URL: ${{ inputs.base_url || secrets.SMOKE_CANDIDATE_BASE_URL }}
          BIKERLINK_DEPLOY_ENV: staging
          DATABASE_URL_CANDIDATE: ${{ secrets.DATABASE_URL_CANDIDATE }}
          SMOKE_REGISTRATION_TOKEN: ${{ secrets.SMOKE_REGISTRATION_TOKEN }}
          SMOKE_ALLOW_PROD: "0"
          SMOKE_CLEANUP_ORPHANS: "1"
          SMOKE_TIMEOUT_MS: "10000"
        run: |
          set -o pipefail
          npx tsx scripts/smoke/run-smoke.ts 2>&1 | tee smoke-report.txt

      - name: Upload smoke report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: smoke-report-${{ github.run_id }}
          path: smoke-report.txt
          if-no-files-found: warn
          retention-days: 14
