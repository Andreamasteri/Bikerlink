name: Deploy Turnistica

on:
  push:
    branches: [main]
    paths:
      - "turnistica/**"
      - ".github/workflows/turnistica-pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: turnistica-pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Configure Pages
        uses: actions/configure-pages@v5
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: turnistica
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
