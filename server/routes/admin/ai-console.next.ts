/**
 * ai-console.next.ts — file successore di ai-console.ts
 *
 * Quando ai-console.ts supererà la soglia delle 600 righe, spostare qui
 * i nuovi blocchi (handler, helper, schema) invece di aggiungerne altri
 * all'originale.
 *
 * Convenzione di utilizzo:
 *   - Aggiungere qui SOLO codice nuovo (non spostare codice esistente da ai-console.ts).
 *   - Esportare dal file e importare in ai-console.ts (o nel router principale) quanto necessario.
 *   - Aggiornare questo commento man mano che il file cresce.
 *
 * Stato attuale di ai-console.ts al momento della creazione di questo file: 502 righe.
 */

export {};
