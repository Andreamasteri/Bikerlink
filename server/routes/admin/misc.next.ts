/**
 * misc.next.ts — file successore di misc.ts
 *
 * Contenuto futuro (da spostare qui quando misc.ts supera la soglia):
 *   - Route road-hazards admin (GET/POST/DELETE)
 *   - Route motion simulator (GET /motion/status, POST /motion/toggle)
 *   - Route motoclub admin (GET /motoclubs, GET /motoclubs/requests)
 *   - Route mass-seed (POST /mass-seed-fake-users, GET /mass-seed-status)
 *   - Eventuali nuove route miscellanee admin
 */

import { Router } from "express";

const router = Router();

export default router;
