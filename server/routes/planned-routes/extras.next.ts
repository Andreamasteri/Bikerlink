/**
 * extras.next.ts — file successore di extras.ts
 *
 * Quando extras.ts supererà la soglia delle 450 righe, spostare qui
 * i nuovi blocchi (handler, helper, schema) invece di aggiungerne altri
 * all'originale.
 *
 * Convenzione di utilizzo:
 *   - Aggiungere qui SOLO codice nuovo (non spostare codice esistente da extras.ts).
 *   - Esportare dal file e importare in extras.ts (o nel router principale) quanto necessario.
 *   - Aggiornare questo commento man mano che il file cresce.
 *
 * Stato attuale di extras.ts al momento della creazione di questo file: 206 righe.
 */

export {};
