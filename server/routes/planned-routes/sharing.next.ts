/**
 * sharing.next.ts — file successore di sharing.ts
 *
 * Quando sharing.ts supererà la soglia delle 450 righe, spostare qui
 * i nuovi blocchi (handler, helper, schema) invece di aggiungerne altri
 * all'originale.
 *
 * Convenzione di utilizzo:
 *   - Aggiungere qui SOLO codice nuovo (non spostare codice esistente da sharing.ts).
 *   - Esportare dal file e importare in sharing.ts (o nel router principale) quanto necessario.
 *   - Aggiornare questo commento man mano che il file cresce.
 *
 * Stato attuale di sharing.ts al momento della creazione di questo file: 241 righe.
 */

export {};
