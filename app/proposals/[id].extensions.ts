/**
 * File successore di app/proposals/[id].tsx
 *
 * Quando app/proposals/[id].tsx supera la soglia delle 600 righe,
 * aggiungere qui i nuovi blocchi di codice (hook, helper, tipi, stili extra)
 * relativi alla schermata ProposalDetail, poi importarli nell'originale.
 *
 * L'originale NON va modificato strutturalmente: spostare solo le parti
 * eccedenti in questo file per mantenere la leggibilità.
 *
 * Convenzione:
 *  - Utility functions  → esportare con nome esplicito
 *  - Tipi/interfacce    → esportare con nome esplicito
 *  - Stili extra        → esportare come oggetto StyleSheet named
 *  - Hook custom        → prefissare con "use"
 */

export {};
