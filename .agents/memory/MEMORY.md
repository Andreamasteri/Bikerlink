- [Repl Layer Size Limit](repl-layer-size.md) — .local/state/replit/ cresce nel tempo (transcript agente + log-query.db) → supera limite Cloud Run 2 GB → deploy silenziosamente fallisce senza log. Include: la pulizia .cache/ deve essere best-effort o `rm` permission-denied fa fallire il build con `set -e`.
- [Publish copia dev DB → prod DB](publish-db-copy.md) — il publish copia il DB dev sopra prod a ogni deploy: sovrascrive dati prod reali + innesca il diff schema PostGIS (spatial_ref_sys). Disattivare l'opzione nella UI di publish.
