- [Nadir manual multi-language design](nadir-manual-multilanguage.md) — Horus translates the IT manual per app language, Nadir indexes+filters each language separately with fallback; admin hand-edits don't refresh translations.
- [Manual translation: Horus fn, Nadir storage](nadir-manual-translate-trigger-location.md) — translateManualToLanguage lives in horus-scanner-finalize.ts; nadir/manual.ts only stores; saveNadirManualTranslation merges one lang without touching others.
- [qwen3 think behavior on Ollama](qwen3-ollama-think-quirk.md) — Horus=4b/Bowie=1.7b reason by default; non-streaming/JSON paths: think:false + strip orphan </think>; STREAMING persona path (agent.ts): think:true for BOTH (ollamaThinkSeparated=true) → reasoning to fullStream, never textStream (see also qwen3-think-false-personas.md).
- [GraphHopper multi-area family](routing-area-mode-legacy-gone.md) — root /info+/route now 404 (multi-area-only TC); routing_area_mode unset silently falls back to the dead legacy path; each GH answers on /areas/<code>/info, none at root (vedi gh-multiarea-info.md).
- [large-files-ratchet scans filesystem, not git](large-files-ratchet-external-refs.md) — untracked reference clones on disk (e.g. .bikerblog-ref) still get scanned by the 600-line gate; add to EXCLUDED_DIRS proactively.
- [Ollama persona vs model param callsites](ollama-persona-model-param-callsites.md) — `persona` in callOllamaChat picks only the endpoint not the model; audit EVERY `persona: "horus"` call site individually for explicit `model:` (also hits ad-hoc inter-agent consult calls, not just the roster).
- [Nadir OpenAI quota circuit breaker](nadir-openai-quota-circuit-breaker.md) — quota-exhausted 429 opens a process-wide breaker (10min) so reindex chunks skip straight to local fallback instead of retrying OpenAI 3x each.
- [Ares/Quebracho lack native tool-calling](ares-quebracho-no-native-tools.md) — no streamText/tools; new in-chat capabilities need pre-composition message interception in agent.ts, not a tool() def.
- [TC GPU boot + persona preload](tc-gpu-boot-persona-preload.md) — nvidia.conf Requires→Wants (no boot-block se GPU assente); GPU non enumerata=hardware/alimentazione, non driver; keep_alive:-1 + OLLAMA_MAX_LOADED_MODELS espliciti per Bowie/Horus/Nadir/Quebracho, Ares escluso.
- [Sentry RN integrations loop](sentry-rn-integrations-loop.md) — @sentry/react-native 8.x default integrations causano loop React Navigation (setState in commitLayoutEffects); usa sempre integrations:[].
- [DragonflyDB flags (compat + BullMQ)](dragonfly-flag-compat.md) — Redis-only flags crashano Dragonfly; usa --snapshot_cron + --maxmemory ≥1gb; BullMQ richiede cluster_mode=emulated + allow-undeclared-keys (vedi dragonfly-bullmq-undeclared-keys.md).
- [ThinkCentre DragonflyDB network path](tc-dragonfly-network-path.md) — DragonflyDB NON va in tunnel Cloudflare come hostname HTTP; nessun path TCP Cloudflare ancora costruito; nginx+DuckDNS:6380 funziona ma è deprecato, non usarlo per TC_DRAGONFLY_URL.
- [FloatingWidget Android hitbox — Reanimated vs RN Animated](floating-widget-android-hitbox.md) — useSharedValue+useAnimatedStyle (Reanimated) NON aggiorna la hitbox touch su Android; usare RN Animated.Value con transform.
- [RN boot-loop guard family](rootlayout-interactionmanager-guard.md) — mount via InteractionManager; auth gate distribuito (mai in _layout); guard `isLoading || !user`; gate mai null senza children — see auth-gate-distribution.md, mapscreen-bootguard-user-null.md, map-ready-gate-null-bug.md, tab-layout-boot-guard.md.
- [React Query + TabBar/memo loop family](react-query-batching-tabbarlayout-loop.md) — TabBar loop: renderCustomTabBar deps:[]+tabBarStateRef (layer1), nested screenOptions objects in useMemo (layer2), InteractionManager sul Tour setVisible (layer3). Memo/handler deps: dipendi da slice primitive (data/.refetch/.isPending), MAI oggetti RQ interi né rimuoverli; mutation intera in renderItem→ref esente da exhaustive-deps; gate check-mutation-object-deps.sh (vedi auth-context-react-query-deps.md, mutation-object-deps-gate.md, react-query-mutation-ref-deps.md).
- [GH RAM_STORE OOM silenzioso su PBF grandi](gh-ramstore-oom.md) — aree > 5 GB PBF: import OK 28+ min, exit 0, ma properties mai scritto → OOM durante flush/CH; fix: rimuovi RAM_STORE (usa MMAP), heap 14g; usa ecuador come banco di prova prima di buildare tutto.
- [vitest lazy-require mock bypass](vitest-cjs-require-mock.md) — lazy `require()` dentro moduli ESM bypassa `vi.mock`; fix: import ESM statico al top del file.
- [OTA high-water mark floor](ota-hwm-floor.md) — logs/ota-hwm.txt è il terzo floor anti-regressione (dopo EAS+buildInfo); aggiornato atomicamente post-publish; tracked in git (!logs/ota-hwm.txt eccezione in .gitignore).
- [ThinkCentre SSH access](thinkcentre-ssh-access.md) — credenziali in env: TC_SSH_HOST/USER/PASSWORD/PORT; usa paramiko + `echo $pwd | sudo -S`. Procedura pronta: skill `thinkcentre-access` (helper `tc.py status|exec`). TC_SSH_HOST ha prefisso `Https://` da strippare.
- [Deploy probe porta 8081 + GET / boot guard](deploy-probe-8081.md) — stack=EXPO → piattaforma attende Metro su 8081; GET / chiama DB durante init → 500. Fix: probe server 8081 + initState guard.
- [EAS build & APK shortcut](eas-build-git-lock.md) — `GIT_INDEX_FILE=/tmp/eas-build-index` bypassa il blocco .git/index.lock; procedura "vai con la build" completa in apk-build-shortcut.md (bump versioni, publish-ota.sh legge versionCode/runtimeVersion da app.json).
- [GitHub sync force-push](github-sync-force-push.md) — credential helper in ~/.gitconfig legge $GITHUB_TOKEN; `git push github main --force` funziona senza workaround; lock stale .git/refs/remotes/github/main.lock non bloccante.
- [Repl Layer Size Limit](repl-layer-size.md) — .local/state/replit/ cresce nel tempo (transcript agente + log-query.db) → supera limite Cloud Run 2 GB → deploy silenziosamente fallisce senza log.
- [Index-drift gate deadlock](index-drift-gate-deadlock.md) — il gate deploy-build.sh DEVE usare --static-only: interrogare il DB live in Phase 2 (pre-migration) blocca il deploy che applicherebbe il fix.
- [Expo (tabs) route pollution](expo-tabs-route-pollution.md) — file helper (es. *.styles.ts) dentro app/(tabs)/ diventano tab-icona rotte; la custom tab bar renderizza ogni state.route. Tenerli in components/.
- [RN inline-object/prop loop family](rnav-screenoptions-nested.md) — qualsiasi oggetto/funzione inline letto da React Navigation crea nuovo ref ogni render → loop "Maximum update depth exceeded"; fix useMemo/useCallback/costante module-level; skill rnav-memo-guard.
- [router-in-useEffect-deps loop](router-in-useEffect-deps.md) — `router` in deps di useEffect che chiama `router.replace/push` → loop "Maximum update depth exceeded"; fix: routerRef+didRedirectRef; colpisce anche listener-setup con deps [router] (BackgroundNotificationHandler, feedback/index, moderator/logs).
- [spatial_ref_sys deploy failure](spatial-ref-sys-deploy.md) — publish fallisce su copy-OFF con ALTER TABLE spatial_ref_sys ADD PRIMARY KEY; è migrazione interna di Replit, non fixabile lato nostro (filtri drizzle.config.ts ignorati). Copy ON cancella i dati prod. Da supporto Replit.
- [Deploy build .cache cleanup](deploy-build-cache.md) — NON fare rm su .cache/ in deploy-build.sh: è un layer gestito dalla piattaforma e contiene file read-only di altro utente → build fallisce con set -e.
- [Routing kill-switch family](routing-kill-switch.md) — env ROUTING_DISABLED vince sul soft toggle DB (unset in prod→comanda l'admin); watchdog deve SKIP non KO→BROKEN via isRoutingExplicitlyDisabled, mai !isRoutingEnabled (vedi anche routing-correctness-killswitch-skip.md).
- [AI stream robustness & Ollama test quirk](ai-stream-robustness.md) — /ai-stream bufferizza+valida Ollama prima di emettere (no output corrotto, fallback Gemini pulito); OLLAMA_URL è settato nell'env → i test del flusso AI DEVONO mockare ollama-client.
- [AI fallback master switch](ai-fallback-master-switch.md) — global `ai_fallback_enabled` (default OFF=ThinkCentre-only, safe direction); gate at provider.ts runWithFallback+resolveModel choke points; setter fail-fast; cloud-cascade tests must mock the switch ON.
- [AI provider chain strategy & free-tier guards](ai-provider-chain-strategy.md) — chain cloud-first Groq→Gemini→OpenAI (Anthropic rimosso) + Ollama rete finale; ogni chiamata (anche stream) DEVE passare da m.scheduler o l'RPM si bypassa.
- [ioredis ETIMEDOUT flooding fix](ioredis-flooding-fix.md) — 3 livelli: retryStrategy:()=>null (zero retry), client.disconnect() nel catch coordinator, console.error filter in server/index.ts per [ioredis] residui.
- [auto_curvy scenic car profile](auto-curvy-profile.md) — quinto profilo "Auto panoramica": Valhalla-only, NESSUN fallback GH (GH cloud farebbe solo auto diretta); fail = 503 esplicito, gated su /api/settings/valhalla-available.
- [Telemetry routing layer](telemetry-routing-layer.md) — geometric road_class sempre attiva; telemetria opzionale (real/my_style) con retry-to-geometric; warning string server↔client (RouteResultCard) deve combaciare.
- [ThinkCentre home server](thinkcentre-home-server.md) — "ThinkCentre"/"server di casa" = mini-PC locale che ospita i servizi self-hosted (GraphHopper, Ollama, Whisper, Nominatim, Uptime Kuma); fallback cloud se spento.
- [ThinkCentre host Java](thinkcentre-host-java.md) — Java di sistema (fuori Docker) = OpenJDK 25 LTS da apt (Ubuntu 26.04 ha openjdk-25-jdk, no Temurin); non è un servizio; graphhopper.service usa symlink /usr/bin/java (segue update-alternatives).
- [GraphHopper build TC ops](graphhopper-build-tc-ops.md) — "grafa" muore subito se non: run script come root (self-redirecting launcher, no --sudo outer-redirect), override SWAP_FILE/BACKUP_DIR (no /mnt/nvme, NVMe è su /), stop ollama (~18GB), sudo-clean graphs/.
- [Mappe Leaflet non Google](maps-leaflet-not-google.md) — tutte le mappe usano Leaflet via WebView + tile OSM (JS puro, OTA-able); react-native-maps/Google Maps escluso apposta (stabilità/peso/costi); "mappa nera" = residuo Google Maps con API key placeholder finta.
- [Black map diagnosis](black-map-diagnosis.md) — 0 map_init su un ramo app_version (GPS continua) = componente mappa non monta (crash render, non WebView); leaflet-rotate era codice morto.
- [Map-visibility null-coords invariant](map-visibility-null-coords-invariant.md) — mai hide_from_map=false con lat/lng NULL (rider "visibile" che non appare mai); signup nasce hidden, revealOnFirstCoordinate (server/lib) svela alla prima coord, login flippa a hidden se nessuna sorgente.
- [Paginated API vs Array useMemo crash](paginated-api-array-guard.md) — /api/proposals restituisce {data:[],total,page} non array puro; useMemo che chiama .filter() sull'oggetto truthy crasha "undefined is not a function". Fix: select: su useQuery + Array.isArray guard nei memo.
- [match-health source probes](match-health-source-probes.md) — 0 match ≠ anomalia: classifica OK/WARN/NO_DATA/INACTIVE; la sonda sorgente DEVE rispecchiare ciò che il matcher consuma; tipi senza producer (es. club_zav id 4) → INACTIVE.
- [OTA publish pipeline & message convention](ota-publish-pipeline.md) — auto-riavvia coi workflow, .ota-message vuoto ripubblica lo stale dal DB, client in lockstep col server; messaggio di OTA N descrive le novità DI N (mai "vX.Y.N-1") — vedi anche ota-message-convention.md.
- [Fresh APK no-regression](ota-apk-no-regression.md) — un APK nuovo da HEAD esegue il bundle embedded e NON può auto-applicare una OTA approved più vecchia (EAS offre solo la più recente del canale + gating server per-approval); pending pubblicate su channel=production, non staging. No runtime bump per il proofing.
- [Metro cache & startup race](metro-blocklist-cache.md) — maxWorkers=4, non cancellare metro-file-map automaticamente; Clean Metro/Watchdog DEVONO essere lock-aware (/tmp/start-metro.lock+pgrep start-expo) prima di killare 8081 — vedi anche metro-startup-race.md.
- [AI strict-mode schemas](ai-strict-schema.md) — schema Zod per generateObject (OpenAI/Groq strict): usa `.nullable()` non `.optional()` e niente object catchall/record (additionalProperties deve essere false), altrimenti il response_format è rifiutato a ogni chiamata.
- [GPS offline buffer rimosso](gps-offline-buffer-removed.md) — buffer GPS client write-only mai riletto saturava AsyncStorage (SQLITE_FULL)→mappa rotta; non reintrodurre senza recovery. Log-retention job config-driven ogni 5gg; system_signals/maps_telemetry già self-clean 7gg.
- [Native module assente nell'APK](native-module-missing-in-apk.md) — dipendenza Expo aggiunta dopo il build del binario = modulo nativo assente; l'API lancia "Cannot find native module" → crash; OTA (solo JS) non lo aggiunge. Guard con requireOptionalNativeModule e degrada.
- [AppSetting valueJson vs value](appsetting-valuejson.md) — upsertAppSetting(key, value?, valueJson?): il collector watchdog legge row.valueJson (JSONB), non row.value. Passare JSON come 2° arg (value stringa) lascia valueJson=null — usare sempre 3° arg per JSON strutturato.
- [Deploy porta 5000 blindata](deploy-port-5000.md) — .replit [deployment] run DEVE usare PORT=5000; PORT=8081 fa fallire l'healthcheck → deploy abortisce. Non editare .replit direttamente: usare deployConfig(). Guard in post-merge.sh rileva la regressione.
- [Package update audit system](package-update-audit.md) — dopo OGNI aggiornamento pacchetti eseguire `npx tsx scripts/audit-package-updates.ts`; Expo usa branch sdk-56 e em-dash nei CHANGELOG.
- [Install research first](install-research-first.md) — prima di installare/configurare qualsiasi software, recuperare sempre docs/repo/changelog/issues aggiornati; le conoscenze di training sono obsolete.
- [Typecheck workflow stale logs](typecheck-stale-logs.md) — typecheck/typecheck-client sono check run-once; gli snapshot /tmp/logs NON si aggiornano al restart → status "failed" stantio; verità = `npx tsc --noEmit -p <proj>` diretto.
- [Routing per-function engine config](routing-function-engines.md) — AppSetting routing_function_engines salva SOLO gli override espliciti (mai i default); "routing" ricade su maps_routing_engine legacy per non regredire. GET e route planner usano la config effettiva.
- [Registry↔migration drift guard](registry-migration-drift-guard.md) — validazione "schema-drift" usa parser table-qualified (no token-search globale) per trovare tabelle/colonne del registry non create da migration; baseline KNOWN_UNMIGRATED tiene verde il drift noto ma blocca il nuovo.
- [600-line ratchet gate](large-files-ratchet-gate.md) — gate blocca a 600; quando si splitta, i file risultanti vanno a ≤450; boot-sequence split in boot-phase3-db-init.ts (Phase 3 DB Init) → 348+235 righe.
- [AI routing engine selector](ai-routing-engine-selector.md) — dual-compare basso usa engine grezzi; part2.ts split richiede re-export esplicito; ogni decision-log entry deve avere metrics attribuite all'engine REALE eseguito.
- [Ollama nginx Host header fix](ollama-nginx-host-header.md) — Ollama 0.24+ ritorna 403 se `Host≠localhost`; fix: `proxy_set_header Host "localhost"` + nginx su `192.168.1.35:443` (no conflitto Tailscale).
- [react-native-keyboard-controller version lock](keyboard-controller-lock.md) — pinned ≥1.21.9 (ora 1.22.1), min per fix Kotlin onConfigurationChanged; in expo.install.exclude; NON scendere sotto 1.21.9.
- [package-lock Replit proxy → EAS crash](package-lock-replit-proxy.md) — dopo ogni `npm install` in Replit, le URL resolved diventano `package-firewall.replit.local` → EAS crashe "Exit handler never called!". Fix: `sed -i 's|http://package-firewall\.replit\.local/npm/|https://registry.npmjs.org/|g' package-lock.json`.
- [Watchdog proposer cooldown — Groq quota burn](watchdog-proposer-cooldown.md) — proposer AI chiama Groq ogni 60s se c'è un problema high/critical persistente (es. Valhalla down) → brucia 200k TPD in poche ore. Fix: cooldown 30min in scheduler.ts.
- [GH Docker entrypoint config fix](gh-docker-entrypoint-config.md) — entrypoint hardcoda `-c config-example.yml` (relativo→bundled car/foot/bike); DEVE override in compose con `entrypoint: ["./graphhopper.sh","-c","/graphhopper/config.yml"]` assoluto.
- [HNSW index deploy strategy](hnsw-deploy-strategy.md) — indice HNSW NON nelle migration né nel dev DB; Replit genera il diff dev↔prod autonomamente → se l'indice è in dev genera CREATE INDEX in prod che fallisce; boot-sequence lo crea al primo avvio.
- [db-integrity schema-registry timeout](db-integrity-schema-registry.md) — information_schema.columns è lento in prod (148 tabelle): N query sequenziali → timeout 30s; fix: getAllColumnsMap() via pg_catalog in 1 query + cache 10min.
- [Local embedding model projection](local-embedding-projection.md) — multilingual-e5-small produce 384 dim; client.ts proietta a 1536 (4× concat + L2-norm); model tag in DB = 'local:Xenova/multilingual-e5-small'; aggiungere alla whitelist db-integrity.
- [Port mapping probe vs Express](port-mapping-probe-vs-express.md) — localPort=5000 DEVE avere externalPort=80; localPort=8081 (probe) externalPort=8081; inverso → tutto il traffico va al probe → text/plain "ok" → API rotte.
- [bg-db-limiter drop vs real error](bg-db-limiter-drop-vs-error.md) — isBgDbLimiterDropError distingue kill-switch/coda-piena/coda-scaduta (WARN, contatore separato) da un errore reale; evita falsi alert watchdog su instabilità DB managed.
- [Cerbero guardiano porte](cerbero-guardian.md) — watchdog.sh ora shim→cerbero.sh; restart backend SOLO se /api/health irraggiungibile (503 init=vivo, no loop); counter crash backend/Metro separati; Clean Metro fast-clean default, FORCE_RESET=1 deep.
- [AI Assistant config contract](ai-assistant-config-contract.md) — config endpoint è PER-PLATFORM ({platform,config}); UI admin deve fetch+PUT android/ios separati; client default-safe `enabled !== false`; UN solo pallino flottante in PanResponder (no RNGH, no AssistantFab separato).
- [DB background-cycle resilience](db-background-resilience.md) — withDbRetry assorbe blip transitori; db-collector escala high/critical solo dopo 3 campioni consecutivi; dedupWarn contro le storm di log.
- [Diagnostica OTA-proof](diagnostic-ota-proof.md) — camera=metodo statico Camera.getCameraPermissionsAsync; sensori girano su ogni build nativa; isDiagnosticApk via Updates.channel (env baked si perde via OTA); canale OTA dedicato `diagnostic`.
- [Tracking fusion gate](tracking-fusion-gate.md) — km distance: shared/tracking-fusion.evaluateSegment è l'UNICO gate (client+server); 3 timestamp GPS distinti (raw-event=heartbeat, usable-fix=freshness, anchor=accept); il reject NON tocca l'anchor o si auto-blocca.
- [DB pool background budget](db-pool-bg-budget.md) — pool max=10 fisso (DB managed Replit); contro la saturazione si riduce la contesa con withBgDbSlot (job bg ≤3 conn, ≥7 riservate al traffico utente), non si ingrandisce il pool.
- [Pool saturation + leak attribution](pool-promise-all-setup-burst.md) — job bg in withBgDbSlot (RE-ENTRANT via ALS) e sequenziali (no Promise.all); pool.connect() diretto satura→API timeout; "saturo ma 0 query attive"=conn idle checked-out → application_name+tracer in-process, statement_timeout=0 su rebuildHnswIndex (vedi pool-leak-attribution.md, find-similar-pool.md).
- [Resume-path async crash hardening](resume-path-async-crash.md) — ErrorBoundary NON copre le rejection dei listener AppState; ogni body va in try/catch + apiRequest({timeoutMs}); markAsyncError per errori async non fatali.
- [Stregatti unified activity toggle](stregatti-activity-toggle.md) — motion+rotation = ONE function via fake_motion_enabled (server/fake-activity.ts); global-visibility OFF cascades+409-locks; single /motion/toggle in stregatti.ts.
- [Silent DESC index drift lint](silent-desc-index-drift-lint.md) — CREATE IF NOT EXISTS DESC/WHERE senza DROP = drift silenzioso; Check B in lint-migration-indexes + gate in deploy-build.sh (no DB).
- [Telemetry collector machine + offline-first](telemetry-collector-machine.md) — useTelemetry = macchina pura (una sola sorgente, transizioni serializzate, AppState solo steera); upload by distance 5km non a timer, marker avanza solo su flush ok, DR anchor frozen/drEstPosRef separato (vedi telemetry-offline-first.md).
- [Vacuum smart + collector dedicated conn](vacuum-smart-collector.md) — vacuum notturno: ANALYZE default, FULL solo su bloat>soglia (AppSetting vacuum_full_bloat_threshold, key db_vacuum_smart_v1); db-collector usa 1 sola conn pool.connect()+early-exit se !isPoolHealthy; SET LOCAL è no-op fuori da txn.
- [react-native-webview TypeScript regression](webview-type-regression.md) — index.d.ts WebView<P=undefined> → never in TS6 strict; patch P={} baked in post-merge.sh; STILL shipped in 14.0.1 (patch survives 13→14; only 14.0 breaking = Android minSdk 24, already met).
- [TS 7 migration path](ts7-migration-blocked-tseslint.md) — TS7 is compiler-clean; the real blocker is any linter whose parser hard-depends on `typescript` internals (e.g. @typescript-eslint); swap linter first.
- [oxlint disable-comment placement](oxlint-disable-comment-placement.md) — for react-hooks/exhaustive-deps, `oxlint-disable-next-line` must sit directly above `}, [deps]);`, single-line only, or suppression silently fails.
- [DB managed-Postgres slowness vs pool leak](db-managed-slowness.md) — ping>8s con waiting=0 = lentezza managed Replit (non leak); allarmi downstream soppressi quando ThinkCentre spento; backlog map-matching → stato terminale 'exhausted'.
- [Boot crash-loop resilience](boot-crash-loop-resilience.md) — DB lento al boot causava loop: seed eager-promise→unhandledRejection (usa thunk), seed che propaga (withDbRetry+catch), exit immediato (backoff Atomics.wait su /tmp).
- [Watchdog high-severity alerts](watchdog-high-severity-alerts.md) — alerts.ts generic loop pushes ONLY critical; "high" signals need a dedicated per-id block + shouldSend throttle to notify admins.
- [Watchdog recovery/all-clear gate](watchdog-recovery-alert-gate.md) — an "all-clear" push read from raw snap.metrics must be latched to a real (non-suppressed) start alert, or admins get a rientrato for an overload suppressed while TC was powered off.
- [Scheduler heartbeat & zombie recovery](scheduler-heartbeat-recovery.md) — ogni tick (anche skip) emette heartbeat; cycleInFlight reset >10min; bg-db-limiter coda con tetto+timeout (dropped* in stats). Evita falsi "high" + burn proposer.
- [Expo typed-routes stale in validation](expo-typedroutes-stale.md) — navigate new routes with `router.push(\`/x/${id}\` as never)`, NOT typed `pathname:"/x/[id]" as const`; validation tsc doesn't regen gitignored .expo types so new routes fail.
- [eslint single-file false unused](eslint-single-file-false-unused.md) — linting one TS file alone falsely flags used type-only imports (method return types) as unused; run the full-project lint instead, never delete them.
- [AI Assistant admin actions](assistant-admin-actions.md) — admin & user chat share extractActions; split by whitelist at filter time; admin ops server-side (role gate+audit), IDs from admin-context snapshot.
- [tabs layout no-split](tabs-layout-no-split.md) — app/(tabs)/_layout.tsx ha marker @no-split (documentale): non ri-splittare in .partN, options Tabs.Screen module-level (statiche o factory), anti-loop = frozenTabScreensRef.
- [Bowie Terminal push signals](bowie-terminal-push-close.md) — data-only pushes (`main_app_foreground_close`, instant-close) speed up the 50s poll fallback on both platforms; iOS needs priority:normal+_contentAvailable+UIBackgroundModes+notification category for visible reply UI, Android doesn't.
- [Boot-loop cached-user hydration](boot-loop-cached-user-hydration.md) — causa boot-loop = redirect ottimistico a /(tabs) con user=undefined; fix = seed cache auth PRIMA di abilitare la query, mai redirect in authIsLoading; gotcha refetchQueries no-op su query enabled:false.
- [Fullscreen overlay Android touch](fullscreen-overlay-android-touch.md) — Animated.View fullscreen mangia i tap dei bottoni su Android: fix box-none + elevation > fratelli flottanti (non solo zIndex); ripristina il blocco modale con Pressable backdrop dietro la card.
- [BootGate activation flag sources](boot-gate-activation-sources.md) — flag "local OR remote" su device: override MANUALE (sticky) e specchio REMOTO (simmetrico) su CHIAVI SEPARATE; mai latchare remoto nel manuale o l'admin toggle-off non si propaga.
- [Custom Docker images TC (Valhalla+GraphHopper)](valhalla-custom-build.md) — build da sorgente sul TC: bikerlink/valhalla (master HEAD, submodule init, version via valhalla_service --version) e bikerlink/graphhopper:latest (GH12.x Java25, graphhopper.sh embeddato, verifica /health DA DENTRO il container) — vedi gh-custom-image-java25.md.
- [/api/health status contract](health-status-contract.md) — status booting/ready/degraded (mai 500); cerbero-lib.sh grep-a la stringa → cambiare il vocabolario in lockstep col monitor+test.
- [Boot post-READY block](boot-postready-block.md) — critical path vs runPostReady() async/non-fatale; MAI process.exit post-READY (crash-loop); .catch() sul void obbligatorio; fallimenti → markDegraded.
- [OTA Emergency channel (EMCY)](ota-emergency-channel.md) — pipeline OTA parallela via flag ota_emergency_active; approve preserva release.channel; publish da git worktree; /tmp e workspace sono FS separati (tar-pipe, no rsync); eas update richiede --environment production (vedi emcy-pipeline-fixes.md).
- [Production data removal is publish-gated](prod-data-removal-publish-gated.md) — agent can't delete live prod rows (prod DB read-only, publish diffs schema-only); only a boot cleanup on user publish mutates prod data; code-review blocks "complete" until rows actually gone.
- [Semgrep CI gate quirks](semgrep-gate.md) — anonymous Semgrep redacts fingerprint/lines to "requires login" (compute own sha1); p/expressjs not p/express; verify via startValidationRun not the 120s bash cap.
- [Merge part files — eslint-disable scope](merge-eslint-disable-scope.md) — la direttiva eslint-disable file-level di un part va riprodotta come blocco disable/enable nel file fuso, non disable-next-line, o il gate --max-warnings=0 fallisce.
- [Split-file merge via bash assembly](split-merge-bash-assembly.md) — fondi i part lazy-split assemblando con sed/cat (no ritrascrizione); subagent lasciano lavoro parziale, fonte di verità = filesystem; marker >600 = LARGE-FILE-ALLOW + voce allow.txt.
- [Ollama diagnostics CLI & instance naming](ollama-diag-cli.md) — `npx tsx scripts/ollama-diagnose.ts` invia log ad Ares (secret DIAG_OLLAMA_*); naming: Ares=DIAG_OLLAMA_*, Bowie/Horus=OLLAMA_* (TC) — vedi anche ollama-naming.md/ollama-model-secret.md.
- [CF tunnel/access troubleshooting](cf-tunnel-origin-down-vs-secret.md) — uniform 502+SSH-timeout = box/tunnel offline (not secret); 403 con client_id+policy ok = secret sbagliato (dev'essere 64-hex no .access, Copy button); dettaglio anche in cf-access-secret-troubleshooting.md.
- [Deep schema parity (dev↔prod)](deep-schema-parity.md) — confronto DEFINIZIONI (non nomi) su 7 categorie via hash; prod catturata offline (no conn string, read-only db skill) in server/data/deep-schema-parity.prod.json; spatial_ref_sys_pkey/postgis/user_sessions_chk allow-listati.
- [TC admin monitor restore](tc-monitor-restore.md) — "monitor rotto dopo migrazione" = secret URL mancanti non codice; agent HTTP probe 401/403=auth non verde (mai s<500); leggi i secret TC per-request; GPU/VRAM via nvidia-smi in stats-server.js; test probes isolano env via PROBE_ENV_VARS+resetProbeEnvForTests (mai delete-list a mano), tcpConnectOk corto-circuita sotto test.
- [Ares access (WoL + Linux migration)](ares-wake-on-lan.md) — WoL via wake-ares.sh (funziona da standby, no apt-get); post-migrazione Linux è LAN-only via ProxyJump dal TC (ares.py, secret ARES_SSH_KEY); dettaglio anche in ares-linux-migration.md.
- [AI tri-persona handoff (Bowie/Horus/Ares)](ai-tri-persona-handoff.md) — handoff deciso nel ROUTE (non agent, test verdi); Ares ha provider dedicato DIAG_OLLAMA_* NON coperto da hasAnyAiProvider() → risolvi persona prima del precheck 503 e salta per ares.
- [ThinkCentre exposure reality](thinkcentre-exposure-reality.md) — nginx disabled/legacy (storia migrazione DuckDNS/nginx→Tunnel + listen 192.168.1.35:443 in thinkcentre-cloudflare-migration.md); esposizione reale via Cloudflare tunnel (biker-link.net); Ollama bind 127.0.0.1; Valhalla ascolta SOLO 8002, ingress→8003 rotto (dashboard CF).
- [Scheduler retry boundary](scheduler-retry-boundary.md) — withSchedulerRetry wrappa SOLO discovery/acquisizione idempotente, mai il loop mutante; bg-db-limiter rigetta ad acquire(); last_attempt separato da last_success, surfaced in admin+watchdog.
- [Bowie notification reply + per-device push](bowie-notification-reply-delivery.md) — app-killed headless reply unreliable; opensAppToForeground:true + getLastNotificationResponse cold-start recovery + dedupe, submit via notification-reply not inline SSE; sendBowieReplyPush(deviceId) targets one device, revoke purges pushTokens (vedi bowie-per-device-push.md).
- [Env vs Secret classification](env-secret-classification.md) — EXPO_PUBLIC_* stay env (client-inlined); server-only service URLs are secrets; Redis=DragonflyDB via TC_REDIS_URL (not Upstash/REDIS_URL); viewEnvVars unfiltered secrets map is stale, use keys-filtered.
- [Secrets ops: cold boot & no delete](updated-secret-cold-boot.md) — cambiare il VALORE di un secret esistente richiede cold boot/deploy/merge (restart workflow non propaga); un secret NUOVO entra subito; nessuna callback deleteSecrets, solo l'utente rimuove dal pannello (vedi no-programmatic-secret-delete.md).
- [Nested Expo app expo-doctor duplicate-deps false positive](bowie-terminal-nested-app-doctor.md) — un'app Expo standalone nested (es. bowie-terminal) dentro un repo che è già un'altra app Expo completa fallirà sempre il check "no duplicate dependencies" (node resolution risale la directory tree); non è un bug reale.
- [Custom migration runner is boot-gated, not publish-diffed](custom-migration-runner-boot-gate.md) — migrations/*.sql run via server/migrate.ts on server boot (not Replit's schema-diff-on-publish); one bad duplicate-prefix file blocks the ENTIRE pending batch (dev AND prod) until fixed.
- [Dual-read env gate audit](dual-read-env-gate-audit.md) — quando aggiungi un fallback NEW ?? OLD per un secret rinominato, controlla OGNI `if (process.env.OLD)` diretto, non solo il wrapper URL; un gate raro (pub/sub coordinator) può restare sul vecchio nome e fallire in silenzio.
- [Secret verification blocked by unrelated boot crash](secret-verify-boot-crash.md) — dopo aver aggiunto un secret nuovo, se il boot crasha PRIMA della fase che lo consuma (es. migration-prefix-guard fatal) va risolto il blocco (anche se ignoto) per poter confermare il secret funzionante.
- [Bowie Terminal iOS quick-reply category](bowie-terminal-ios-reply-category.md) — iOS push token alone (silent pushes) isn't enough for visible reply UI; needs requestPermissionsAsync + setNotificationCategoryAsync; also covers the static-review+checklist pattern for iOS-hardware-only verification tasks.
- [Bowie/nested-Expo EAS archive pollution — root cause e fix reale](bowie-eas-monorepo-archive.md) — EAS_NO_VCS=1 da solo non basta (noVcs fa comunque git rev-parse); serve anche EAS_PROJECT_ROOT assoluto o scansiona l'intero monorepo.
- [Matching Coordinator control plane](matching-coordinator-control-plane.md) — gate unico "può girare un ciclo ora?"; Horus scrive (pause/resume/force_cycle), Bowie solo legge/relaya; fallback trasparente se Horus/TC irraggiungibile.
- [Sanitize order — secret before PII](sanitize-secret-before-pii.md) — check matchesSensitive sul testo GREZZO prima di redactPII: la redazione PII può spezzare un token e far trapelare un frammento di secret.
- [Persona intro poem pattern](persona-intro-poem-pattern.md) — one-time greeting via persisted union-list `introShownPersonas`, not sticky `previousPersona`; mark-shown must NOT be gated on stickiness/farewell.
- [Date.now mock fragility with vi.resetModules](datenow-mock-fragility.md) — call-count/parity Date.now() mocks break when vi.resetModules() re-triggers module-scope side effects (e.g. Bottleneck) that also call Date.now(); use a controllable clock keyed to an identifiable call site instead.
- [AI SDK v6→v7 migration](ai-sdk-v6-to-v7-migration.md) — only `tool<IN,OUT>()` generics + `as never` on tools actually break; system→instructions/stepCountIs→isStepCount/onFinish→onEnd are soft aliases; generateStructured `system` is OUR wrapper not the SDK; runs fine on Node 20 despite engines>=22.
- [AI agent model-default staleness](ai-agent-reconnection-model-defaults.md) — "disconnesso" con endpoint 200 = default model non esiste su `ollama list` (secret *_OLLAMA_MODEL unset → default comanda); fixa il nome non l'auth; audit via tsx bypassando HTTP auth, doc "critical patterns" (think:false) spesso stale — vedi ai-agent-endpoint-audit.md.
- [Horus routing-correctness namespace + AI priority gate](horus-routing-correctness-namespace.md) — probes under SignalSource "horus", isolated from generic proposer; in-process gate gives routing-AI priority since self-hosted Ollama bypasses Bottleneck.
- [Quebracho control-plane family](quebracho-coordinator-gate.md) — canRunJob mai blocca-per-sempre (pause admin rispettate, pause AI ignorate se irraggiungibile); issuer paritari quebracho/horus/admin_manual stesso fallback; withJobGate generic sul return type, multi-authority pause = slot PER issuer (vedi horus-autonomous-directive-pattern.md, quebracho-job-gate-wiring.md).
- [AI assistant tool-calling port](ai-assistant-tool-calling-port.md) — BikerLink usa AI SDK streamText (non chatRaw loop); porta i fix come pure helper in tool-calling.ts; cloud fallback gira SENZA tool → guard se il turno richiede tool; #4/#5/#10/#11 N/A, #9 già corretto.
- [drizzle-orm sql mock gap](drizzle-sql-mock-agent-import.md) — ogni test che importa l'assistant agent tira dentro db-integrity/counters.ts che usa `sql` a module-scope; il vi.mock di drizzle-orm DEVE esportare `sql` o l'intera suite fallisce all'import.
- [drizzle sql array → IN vs ALL/ANY](drizzle-sql-array-all-any.md) — `${jsArray}` in un template `sql` diventa `$1,$2,$3` (ok per IN, ROTTO per ALL/ANY che vogliono un array Postgres); usa notInArray/inArray del query builder.
- [SSE abort res-not-req](sse-abort-res-not-req.md) — su Node 20 + express.json() `req` "close" scatta al parse del body ed è one-shot → abort su disconnessione morto; usa SEMPRE `res.on("close")` per abortire uno stream AI (BikerBlog parità E1).
- [Expo web preview disabled — render testing](expo-web-preview-disabled-render-testing.md) — no Playwright/device e2e possible; use react-test-renderer+real QueryClientProvider, IS_REACT_ACT_ENVIRONMENT flag, separate act() flush ticks.
- [Multi-agent group conversation](group-conversation-multiagent.md) — tavola rotonda Horus/Bowie/Quebracho: prompt apertura≠risposta (o allucina battute altrui), storia come UN solo msg user, turnCount persistiti = fonte ripresa, surface=group su ai_call_logs.
- [Database Monitor history](db-monitor-history.md) — DB+backend load history is its own 35d table (separate from signals 7d/resource 24h), written per aggregator tick via withBgDbSlot; watchdog metric keys are double-prefixed (source+metric).
- [Inter-agent tools + task review](inter-agent-tools-review.md) — gating a 3 livelli (mai allargare); no timeout corto su tool heavy-model; file-read in chat = arbitrary-read (confina root + admin); memoria Horus GLOBALE = write solo-admin + reject-secret/redact-PII; memory path lazy.
- [Prod data-check via executeSql production](prod-data-check-methodology.md) — niente connection string prod: usa `executeSql({environment:"production"})` SELECT-only, batch UNION ALL per categoria di check, non fidarsi di `pg_stat_user_tables` stale.
- [Expose new TC service w/o CF ingress](tc-agent-localhost-proxy-pattern.md) — bind localhost + reverse-proxy `/<nome>/*` dentro thinkcentre-agent (già su tc.biker-link.net, CF Access + X-Agent-Token); l'agente non può creare ingress CF. Es: kalman service.
- [Telemetry stats incremental summary](telemetry-stats-incremental.md) — GET /api/telemetry/stats reads per-session summary table (telemetry_session_stats), not a Haversine scan; ANY ride_telemetry mutation must keep the summary in sync or totals drift; old 60s cache removed.
- [TC 502 monitors — 3 unrelated causes](tc-cloudflare-tunnel-502-root-causes.md) — Valhalla stale port, GH multi-area never proxied + sites-enabled≠sites-available drift + token drift, TC agent process not running.
- [Inter-agent consult persona/model mismatch](inter-agent-consult-model-mismatch.md) — callOllamaChat risolveva il modello solo dal default Bowie; ogni consult persona-specifico deve passare `model` esplicito o colpisce silenziosamente l'endpoint sbagliato.
- [Ares/devstral cold-load latency](ares-devstral-cold-load-latency.md) — 55-170s per rispondere (CPU-bound, 14GB); timeout ARES ≥170s, non riabbassare senza rimisurare live.
- [detect-secrets baseline drift](detect-secrets-baseline-drift.md) — secrets-scan can flag untouched lines as new even when baseline+hash match exactly; verify against pristine main before treating as a regression.
- [Persona handoff vs consult-tool priority conflict](persona-handoff-vs-consult-tool-priority.md) — roster.ts (handoff pieno) e tool-calling.ts (call_horus/quebracho/ares) hanno regex quasi identiche; roster vince sempre, può rendere il tool consult irraggiungibile per la sua stessa frase-esempio.
- [Live session cookie testing](live-session-cookie-testing.md) — bearer curl valido = riga `session` con `{cookie,userId}` FLAT (non passport.user); platform enum è android/ios/web/admin, mai "mobile".
- [DR correction engine](dr-correction-engine.md) — deterministic (NON-AI) dead-reckoning/GPS drift correction; DR gap stays RAW (learning ground-truth), scale only the live total; per-user real-time + global 6h job; 2–3 coherent-fix recovery gate; is_test set server-side; keep out of server/ai/*.
- [Ares long-running jobs](ares-jobs-design.md) — analysis+manual on-demand background jobs (server/ai/ares-jobs); indipendenti da Horus (AppSetting store, no aiAnalysisRuns); manuale→storage Nadir+reindex+backup; chunk budget deve coprire ~2000 file sotto il cap; interactive-priority gate.
- [Horus full-app scan on-demand](horus-full-scan-ondemand.md) — due scansioni STRETTAMENTE on-demand (analisi codice+DB→proposte, manuale→storage Nadir); mai schedulare; fingerprint per-file in AppSettings; interruzione pulita se Ollama giù; manuale condivide NADIR_MANUAL_PREVIOUS_KEY con Ares.
- [TC LAN subnet drift](tc-lan-subnet-drift.md) — TC's real LAN subnet is 192.168.0.0/24 (not .1.0/24); stale ufw rule/comments block real local SSH silently (timeout, not refused).
- [TC app-repo stale → wrong Ollama model build](tc-app-repo-stale-model-build.md) — ~/bikerlink on the TC lags origin/main by ~hundreds of commits; `ollama create` from it uses stale Modelfiles (built FROM mistral-nemo not qwen3:1.7b). git checkout origin/main -- <modelfile> first. BOWIE/HORUS_OLLAMA_MODEL are shared env vars not secrets.
- [Storage↔embeddings circular dep](storage-embeddings-cycle.md) — importing one storage class alone crashes "Class extends undefined"; real back-edge is leaf `embeddings/store.ts`→aggregate `../storage` singleton (now lazy `await import`), NOT a test-mock issue.
- [Test DB-mock & static-scan drift](test-db-mock-and-static-scan-drift.md) — shared opt-in helper server/__tests__/helpers/db-mock.ts (createDbMock, async-dynamic-import factory, Proxy chainable+thenable) is now the single update-point for ../db shape; keep importOriginal for real-withDbRetry suites; ratchet file-splits break path-based source-scans & protection-filter allowlist; isolated storage-class import exposes circular deps.
- [Reserved-name blocking family](reserved-nickname-agent-names.md) — two blacklist shapes coexist on purpose: EXACT match for admin/mod (unchanged), CONTAINS match for internal AI agent names (Ares/Nadir/Bowie/Quebracho/Horus); any new user-creation path (signup, admin-create, profile-rename) must call the shared helper or it's a silent bypass.
