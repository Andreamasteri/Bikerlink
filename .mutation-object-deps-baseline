# .mutation-object-deps-baseline
# Occorrenze legacy congelate dell'oggetto-mutation intero nei deps di
# useCallback/useMemo. Il gate (scripts/check-mutation-object-deps.sh)
# fallisce solo su occorrenze NUOVE non elencate qui.
# Formato (un record per riga):  <path>\t<token>
# Aggiornare SOLO via:
#   BIKERLINK_HUMAN_BASELINE_UPDATE=1 bash scripts/check-mutation-object-deps.sh --update-baseline
