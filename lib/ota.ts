// ⚠️ CHECKLIST RELEASE: aggiornare questo numero PRIMA di ogni pubblicazione OTA
// Ciclo 8.0.0 — APK v42 3.1.0 — OTA-15 distribuisce il set completo di fix audio:
// task #1061 (STOP audio + sovrapposizione tracce), #1064 (costanti AUDIO_MODE_ACTIVE/
// INACTIVE centralizzate), #1065 (audio in background + controlli lock screen via
// foreground service Android). Aggiornare ad ogni nuova OTA pubblicata.
export const CURRENT_OTA_NUMBER = 15;
