// ⚠️ CHECKLIST RELEASE: aggiornare questo numero PRIMA di ogni pubblicazione OTA
// Ciclo 8.0.0 — APK v42 3.1.0 — bundle embedded include fix audio (shouldPlayInBackground)
// e fix protocollo OTA (expo-protocol-version: 0). OTA-14 è l'ultima OTA pubblicata
// del ciclo precedente; con v42 il bundle embedded rappresenta lo stato post-OTA-14.
export const CURRENT_OTA_NUMBER = 14;
