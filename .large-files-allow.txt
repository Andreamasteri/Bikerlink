# .large-files-allow.txt
# Lista chiusa di file TypeScript autorizzati a superare il limite di 600 righe
# tramite marker `// LARGE-FILE-ALLOW: <motivo>` in prima riga.
#
# Formato: un path per riga, relativo alla root del repo.
# Sono ammessi commenti che iniziano con `#`. Righe vuote ignorate.
#
# Auto-discovery PROIBITA: un file con marker LARGE-FILE-ALLOW non presente
# qui fa fallire il gate (`scripts/check-large-files-ratchet.sh`).
# Aggiunte a questa lista richiedono un task utente esplicito.
#
# Vedi `replit.md` → sezione "⛔ REGOLA FERREA — Limite 600 righe per file".

# ── File di traduzione i18n ────────────────────────────────────────────────
# Struttura piatta inevitabile: una chiave-valore per riga, nessuna logica.
# Splitta meno di 150 chiavi per file è rumore senza beneficio.
lib/i18n/de.ts
lib/i18n/el.ts
lib/i18n/en.ts
lib/i18n/es.ts
lib/i18n/fr.ts
lib/i18n/it.ts
lib/i18n/tr.ts

# ── File di test ───────────────────────────────────────────────────────────
# Test estesi per funzionalità critica (privacy-filter): copertura completa
# richiede molti casi d'uso separati; refactoring in più file romperebbe
# la leggibilità della suite.
server/__tests__/privacy-filter.test.ts
server/__tests__/privacy-filter.part2.test.ts

# ── Hook schermata mappa home ──────────────────────────────────────────────
# Catena split (states/handlers/calculated/helpers) fusa in un unico file:
# la logica è fortemente accoppiata (stesso set di stati, refs e handler
# condivisi) e gli split precedenti erano file companion fragili/orfani.
hooks/home/useHomeMapState.ts

# ── Schermate navigazione (merge split part2/3/4 → file unico) ──────────────
# Catene split riunite in un solo file marcato `// @no-split`: stili, helper e
# sotto-componenti UI sono accoppiati alla schermata, nessuno split utile.
app/giri/[id].tsx
app/motoclub/create.tsx
app/admin/stregatti.tsx

# ── Merge split lazy-load → file unici (refactor #5101) ─────────────────────
# Catene split (part2/parts/map/voice) riunite nel file principale marcato
# `// LARGE-FILE-ALLOW`: stili, helper e sotto-componenti sono accoppiati alla
# schermata/hook, gli split erano companion fragili. Nessuno split utile.
hooks/navigate/useNavigateState.ts
components/tracking/TrackingDashboard.tsx
components/admin/users/UserDetailModal.tsx

# ── Agente AI Assistant core ───────────────────────────────────────────────
# Split vietato per decisione esplicita dell'utente (lug 2026): flusso
# streaming unico e coeso (persona, fallback chain, memoria conversazionale).
server/ai/assistant/agent.ts

# ── Route admin AI watchdog ────────────────────────────────────────────────
# ~20 endpoint watchdog in un'unica route — split crea accoppiamento inutile.
server/routes/admin/ai-watchdog.ts

# ── GraphHopper client ─────────────────────────────────────────────────────
# Client multi-area + profili multipli — split richiederebbe re-export artificiali.
server/graphhopper-client.ts

# ── Test suite hub-tools Horus ─────────────────────────────────────────────
# ~20 test case correlati — split spezza la leggibilità della suite.
server/__tests__/ai-assistant-agent-horus-tools.test.ts
scripts/log-analysis-horus.ts
