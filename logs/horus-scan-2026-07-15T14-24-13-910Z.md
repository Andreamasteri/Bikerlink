# Horus — Scansione completa codice+DB

Run: 811a9bb7-7556-4eac-9cf6-cb0fbcd558b4
Data: 2026-07-15T14:24:13.910Z

## Proposte da analisi codice+DB

Okay, let me try to work through this step by step. The user wants me to act as Horus in ANALISI (SOLA LETTURA) mode for the BikerLink app. They provided the current state of DB integrity checks and some partial proposals from 12 files. I need to unify these into a single list of actionable proposals in Italian, ordered by impact, without duplicates, and include any DB schema/coherence issues.

First, I'll parse the DB integrity status. The latest run was on 2026-07-15 at 01:00:00.018Z with yellow health. There are 2 violations (0 auto-fixed, 2 manual), but the check results show multiple entries of "Tabelle nel DB ma non nel registry (drift inverso) (1 righe)". Wait, the user says 2 violations (0 auto-fixed, 2 manual), but the list shows 19 entries of the same type? Wait, the list says:

"[medium/schema-registry] Tabelle nel DB ma non nel registry (drift inverso) (1 righe);" repeated 19 times.

Hmm, that's a bit confusing. Maybe it's a typo in the user's message. The actual DB integrity status says 2 violations, but the list shows 19 identical entries. Probably, the user meant that there are 19 violations of the same type, but the health is yellow with 2 violations? Wait, the user says: "2 violazioni (0 auto-fixed, 2 manuali), check eseguiti=67". So there are 2 violations total. But the list shows 19 entries, each being a "drift inverso" (reverse drift) of 1 row. Maybe it's a mistake in the user's input, but I have to work with what's given.

The key point here is that the DB has drift issues: tables exist in the DB but not in the registry (reverse drift). The problem is that the registry (probably a schema registry) doesn't have some tables that are in the DB. This is a "drift inverso" (reverse drift) because the DB has tables that the registry doesn't. This is a schema drift problem where the DB schema has changed without updating the registry.

Now, the user provided "PROPOSTE PARZIALI" from 12 files. The user pasted some observations from different files, but the proposals are partial. They want me to unify these into a single list of actionable proposals, without duplicates, ordered by impact.

Let me look at the PROPOSTE PARZIALI section. The user describes observations for several files:

- app/(auth)/login.tsx: too broad error handling (ignores errors)
- app/(auth)/splash.tsx: unused variable `bottomInset`, race condition with Promise.race
- app/(auth)/register.tsx: too many state variables
- app/(auth)/forgot-password.tsx: incorrect email regex (doesn't allow domains with multiple dots)
- app/(auth)/verify-email.tsx: default tab might be incorrect
- app/(tabs)/arcade.tsx: default tab behavior
- app/(tabs)/chat.tsx: query timeout too short (15s)
- components/AlwaysPermissionNotice.tsx: Linking.openSettings might not work on all platforms
- hooks/admin/ai-console/useAiAlerts.ts: code smell in date formatting

The user wants to aggregate these into up to 5 actionable proposals in Italian, ordered by impact. Also, they want to include any DB schema/coherence issues (like schema vs migration, indexes, drift, orphaned tables/columns) based on the DB state.

First, let's handle the DB integrity part. The DB has a drift issue: tables in
