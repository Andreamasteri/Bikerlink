#!/bin/bash
# Il dev server Expo/Metro non è più in uso.
# L'app viene distribuita tramite EAS build e OTA updates.
# Backup originale in: scripts/backup/start-expo.sh.bak
echo "[$(date '+%Y-%m-%d %H:%M:%S')] Frontend dev server non più in uso — nessuna azione eseguita."
exit 0
