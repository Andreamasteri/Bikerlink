/**
 * SUCCESSORE DI app/route/[id].tsx
 * ─────────────────────────────────────────────────────────────────────────────
 * Questo file è il companion di `app/route/[id].tsx` (552 righe al 2026-05-28).
 *
 * SCOPO: ricevere nuovi blocchi di codice quando `[id].tsx` supererà la soglia
 * di 600 righe. Esempi di candidati futuri:
 *   - StyleSheet aggiuntivi estratti da [id].tsx
 *   - Nuovi sotto-componenti o helper specifici della schermata RouteDetail
 *   - Logiche di formattazione/calcolo isolabili
 *
 * REGOLA: non modificare `[id].tsx` per fare riferimento a questo file
 * finché non si decide concretamente lo split. Aggiungere qui il nuovo codice,
 * poi aggiornare gli import in [id].tsx in un'unica PR dedicata.
 * ─────────────────────────────────────────────────────────────────────────────
 */

export {};
