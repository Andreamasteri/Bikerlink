/**
 * useHomeMapState.helpers.ts
 *
 * File successore / companion di useHomeMapState.ts (558 righe).
 * Quando useHomeMapState.ts si avvicina o supera la soglia 600 righe,
 * spostare qui i nuovi blocchi logici (helpers, handler, effetti) e
 * riesportarli dall'hook principale.
 *
 * Convenzione:
 *   - Ogni funzione/hook estratto deve mantenere la firma originale.
 *   - Importare da questo file in useHomeMapState.ts, non viceversa.
 *   - Quando anche questo file supera ~550 righe, creare
 *     useHomeMapState.helpers2.ts seguendo la stessa convenzione.
 */

export {};
